{% extends 'base.html.twig' %}

{% block contenido %}
{% block topic %}{% endblock %}
  <div class="container">

     <div class="panel table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Fecha</th>
                    <th>Titulo</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for hecho in hechos %}
                <tr>
                     <td><a href="{{ path('hecho_show', { 'id': hecho.id }) }}">{{ hecho.id }}</a></td>
                    <td>{{ hecho.fecha|date('d-m-Y') }}</td>
                    <td>{{ hecho.titulo }}</td>

                    <td>
                        <ul>
                            <li>
                                <a href="{{ path('hecho_show', { 'id': hecho.id }) }}">ver</a>
                            </li>
                            <li>
                                <a href="{{ path('hecho_edit', { 'id': hecho.id }) }}">editar</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

 </div>

    <div class="row">
        <div class="pull-left">
            <a class="btn btn-default" href="{{ path('hecho_new') }}">Agregar hecho</a>
        </div>

        <div class="pull-right">
            <a class="btn btn-default" href="{{ path('tag_index') }}">Administrar Tags</a>

            <a class="btn btn-default" target="_blank" href="{{ path('elfinder') }}">Administrar Archivos</a>
        </div>

    </div>

{% endblock %}
